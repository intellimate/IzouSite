#!/usr/bin/env bash

# Run this script once the first time you download the repository to install a few packages globally that you might
# need to do various things like testing or deploying code
sudo npm install -g babel-eslint
sudo npm install -g rollup
sudo npm install -g mocha
sudo npm install -g eslint

